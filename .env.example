# Copy this file to .env and replace the placeholder values with your Telegram
# bot token and target chat ID.  Never commit actual secrets to your
# repository.  See README.md for instructions on obtaining these values.

# Telegram bot token provided by BotFather
BOT_TOKEN=your-telegram-bot-token

# ID of the chat (user or group) where the submissions will be sent
TELEGRAM_CHAT_ID=your-chat-id

# Port for the Express server (defaults to 3000 if not set)
#PORT=3000
